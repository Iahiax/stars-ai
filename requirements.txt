# ══════════════════════════════════════════════════════════════
#  Stars AI — متطلبات المشروع الكاملة
# ══════════════════════════════════════════════════════════════

# ── PyTorch (التدريب والاستدلال) ──────────────────────────────
torch>=2.3.0
torchvision>=0.18.0

# ── HuggingFace (النماذج والبيانات والـ Fine-tuning) ──────────
transformers>=4.41.0
huggingface-hub>=0.23.0
datasets>=2.20.0
tokenizers>=0.19.0
sentencepiece>=0.2.0
accelerate>=0.30.0

# ── LoRA والـ Fine-tuning الفعّال ──────────────────────────────
peft>=0.11.0
bitsandbytes>=0.43.0        # تكميم 4-bit (GPU فقط — اختياري)

# ── GGUF والاستدلال المحلي ────────────────────────────────────
llama-cpp-python>=0.2.80    # تشغيل ملفات GGUF على CPU/GPU

# ── CrewAI وLangChain (نظام Multi-Agent Swarm) ────────────────
crewai>=0.30.0
langchain>=0.2.0
langchain-openai>=0.1.0
langchain-anthropic>=0.1.0
langchain-community>=0.2.0

# ── APIs النماذج الخارجية ────────────────────────────────────
openai>=1.30.0
anthropic>=0.28.0

# ── طلبات HTTP (لـ RAG وتوليد البيانات) ────────────────────────
aiohttp>=3.9.0
requests>=2.32.0

# ── Google Cloud (Secret Manager) ────────────────────────────
google-cloud-secret-manager>=2.20.0

# ── أدوات مساعدة ─────────────────────────────────────────────
python-dotenv>=1.0.0
tqdm>=4.66.0
numpy>=1.26.0
