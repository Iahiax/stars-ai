# ══════════════════════════════════════════════════════════════
#  Stars AI — متطلبات المشروع الكاملة
#  طريقة التثبيت الصحيحة في Replit:
#    uv pip install --python .venv/bin/python -r requirements.txt
#  أو راجع ملف install.sh لتثبيت كل شيء دفعةً واحدة
# ══════════════════════════════════════════════════════════════

# ── PyTorch ────────────────────────────────────────────────────
torch>=2.3.0

# ── HuggingFace ────────────────────────────────────────────────
transformers>=4.41.0
huggingface-hub>=0.23.0
datasets>=2.20.0
tokenizers>=0.19.0
sentencepiece>=0.2.0
accelerate>=0.30.0

# ── LoRA والـ Fine-tuning ──────────────────────────────────────
peft>=0.11.0
bitsandbytes>=0.43.0        # GPU فقط — اختياري

# ── GGUF (اختياري) ────────────────────────────────────────────
# llama-cpp-python>=0.2.80  # فعّله إذا أردت تشغيل ملفات GGUF

# ── Multi-Agent (اختياري) ──────────────────────────────────────
# crewai>=0.30.0
# langchain>=0.2.0
# langchain-openai>=0.1.0
# langchain-anthropic>=0.1.0

# ── APIs النماذج الخارجية ──────────────────────────────────────
openai>=1.30.0
anthropic>=0.28.0

# ── HTTP والشبكة ───────────────────────────────────────────────
aiohttp>=3.9.0
requests>=2.32.0

# ── تتبع التدريب ──────────────────────────────────────────────
wandb>=0.17.0

# ── Google Cloud (اختياري) ────────────────────────────────────
# google-cloud-secret-manager>=2.20.0

# ── أدوات مساعدة ──────────────────────────────────────────────
python-dotenv>=1.0.0
tqdm>=4.66.0
numpy>=1.26.0
